import { Router } from 'express';

const router = Router();

// Internal/Authenticated routes will be added here
// Note: Authentication middleware should be applied at the router level in index.ts or individually here

export default router;
